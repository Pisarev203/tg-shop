meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  command: bash -c "python -m uvicorn main:app --host 0.0.0.0 --port 5000 & python bot.py"
  containerPort: 5000
